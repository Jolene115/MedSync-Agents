# System
.DS_Store
*.pyc
__pycache__

# Python Environments
venv/
.venv/
.env

# Editor Settings
.vscode/
.idea/

# Data (CRITICAL: Do not commit raw PHI data)
data/raw/
data/*.zip

# Ignore large raw source files (e.g. CHARTEVENTS.csv)
# BUT allow ADMISSIONS.csv and PATIENTS.csv (small, needed by agents)
# and allow processed/ directory (needed for vitals trends)
